// we cam´n define our own type as well

type User={
    name:string,
    age:number
}
const myUser:User={
    name:'anbu',
    age:30,
    // if we try to add another property it gives an error
   // location:'chennai'
}
console.log(myUser)